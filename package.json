{
  "name": "looper-rage-pad",
  "version": "1.0.0",
  "private": true,
  "description": "Looper // Rage Pad — Web Audio DJ-looper / beat sampler packaged as an Android APK via Capacitor.",
  "scripts": {
    "build:web": "node scripts/build-web.mjs",
    "verify": "node scripts/verify.mjs",
    "icons": "node scripts/gen-icon.mjs",
    "assets": "npm run icons && capacitor-assets generate --android",
    "sync": "npm run build:web && cap sync android",
    "add:android": "cap add android",
    "open:android": "cap open android",
    "apk:debug": "npm run sync && cd android && ./gradlew assembleDebug",
    "apk:release": "npm run sync && cd android && ./gradlew assembleRelease"
  },
  "dependencies": {
    "@capacitor/android": "^8.0.0",
    "@capacitor/core": "^8.0.0"
  },
  "devDependencies": {
    "@capacitor/assets": "^3.0.5",
    "@capacitor/cli": "^8.0.0",
    "playwright": "^1.56.1"
  }
}
